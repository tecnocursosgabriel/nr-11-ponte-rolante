# Dependências locais (não vão para o site)
node_modules/

# Materiais grandes (manter só localmente)
*.pptx
*.pdf
videos/

# Áudios gerados localmente — MP3 devem ir para o GitHub Pages
audios/.text-hashes.json
.env
.env.local

# Arquivos temporários do simulador de atividade
.cursor-activity/

# Sistema
.DS_Store
Thumbs.db
.git/index.lock
